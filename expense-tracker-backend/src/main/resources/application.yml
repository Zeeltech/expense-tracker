spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/expense_tracker
    username: postgres
    password: Zeel@4791
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect




